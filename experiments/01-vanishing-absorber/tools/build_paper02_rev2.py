"""Create Paper-02 manuscript Rev. 2 from frozen canonical Rev. 1.

This script implements only the corrections listed in
PAPER02_REV1_ADVERSARIAL_REVIEW_2026-08-15.md.  Rev. 1 is never modified.
Every replacement is asserted so a source drift fails loudly rather than
silently producing a partial revision.
"""

from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SRC = ROOT / "PAPER02_MANUSCRIPT_REV1_ANON_2026-08-15.tex"
DST = ROOT / "PAPER02_MANUSCRIPT_REV2_ANON_2026-08-15.tex"

text = SRC.read_text(encoding="utf-8")


def replace_once(old: str, new: str):
    global text
    n = text.count(old)
    if n != 1:
        raise RuntimeError(f"Expected exactly one occurrence, found {n}: {old[:100]!r}")
    text = text.replace(old, new, 1)


replace_once(
    r"\title{Electrostatic heterogeneity as apparent diffusion in wavelength-resolved photodetector transport}",
    r"\title{Apparent diffusion from deterministic velocity gradients in wavelength-resolved photodetectors}",
)

replace_once(
    "Finite calibrated generation-depth kernels that overlap a spatially nonuniform velocity region can yield a positive effective diffusion coefficient when the resulting Shockley--Ramo terminal-current response is fit with a homogeneous drift--diffusion model.",
    "Finite generation-depth kernels that are treated as known by the inverse and overlap a spatially nonuniform velocity region can yield a positive effective diffusion coefficient when the resulting Shockley--Ramo terminal-current response is fit with a homogeneous drift--diffusion model.",
)

replace_once(
    "Here we isolate a specific material-attribution failure mode in a wavelength-resolved Shockley--Ramo measurement.",
    "Here we isolate a specific material-attribution failure mode in a wavelength-resolved Shockley--Ramo measurement \cite{shockley1938,ramo1939}.",
)

replace_once(
    "For deterministic downstream velocity $v(z)>0$, zero microscopic diffusion, zero recombination, and the planar weighting potential $\\phi_w=z/L$, the Shockley--Ramo path integral can be written",
    "For deterministic downstream velocity $v(z)>0$, zero microscopic diffusion, zero recombination, and the planar weighting potential $\\phi_w=z/L$, the Shockley--Ramo path integral \cite{shockley1938,ramo1939} can be written",
)

needle = r"""For constant velocity, the source-coordinate difference response is one exact spatial exponential. A velocity gradient changes the source-coordinate law even though every trajectory remains deterministic.

\subsection{Finite calibrated generation kernels}"""
insert = r"""The affine offset in $H$ means that the local spatial exponential is carried by the source derivative rather than by $H$ itself. Define
\begin{equation}
P(z,\omega)=\frac{\partial H}{\partial z}
\end{equation}
and the infinitesimal point-source spatial exponent
\begin{equation}
\boxed{
\gamma_{\rm loc}(z,\omega)
=-\frac{\partial}{\partial z}
\ln[-L P(z,\omega)].
}
\label{eq:gammaloc}
\end{equation}
For a finite source-coordinate step $h$, the corresponding local difference exponent is
\begin{equation}
\gamma_h(z,\omega)
=-\frac{1}{h}
\ln\!\left[\frac{P(z+h,\omega)}{P(z,\omega)}\right],
\end{equation}
which approaches Eq.~\eqref{eq:gammaloc} as $h\rightarrow0$.  This is the point-source limit of the first-difference exponential structure used by the spectral inverse.  The finite-kernel results below do not follow by evaluating $\gamma_{\rm loc}$ at a kernel mean; they require the separate kernel averaging in Eq.~\eqref{eq:channel}.

For constant velocity, the source-coordinate difference response is one exact spatial exponential. A velocity gradient changes the source-coordinate law even though every trajectory remains deterministic.

\subsection{Finite generation kernels treated as known by the inverse}"""
replace_once(needle, insert)

needle = r"""This form retains the independently calibrated channel shapes rather than replacing them with delta functions or assuming rigid translations.

\subsection{Homogeneous inverse model}"""
insert = r"""This form retains the independently specified channel shapes rather than replacing them with delta functions or assuming rigid translations.  Throughout the mathematical analysis, ``known'' or ``calibrated'' kernel means that $g_m(z)$ is supplied to the inverse as an independently characterized forward-model input.  No experimental kernel calibration is performed in this work.

For the conditional HgCdTe numerical example we use six normalized finite kernels generated by the repository's monotonic graded-HgCdTe optical model.  The wavelength of each channel is chosen numerically so that the resulting normalized kernel has mean depth $2.0$, $2.5$, $3.0$, $3.5$, $4.0$, or $4.5\,\mu\mathrm{m}$ in the $7.6\,\mu\mathrm{m}$ absorber.  These theoretical kernels are used exactly in both the forward average and the kernel-aware inverse.  Their purpose is to provide a physically motivated finite-support family for the identifiability stress, not to represent measured optical kernels from a specific detector.  Uncertainty in the kernel model, optical interference, and experimental calibration error are not included in the central counterexample and are discussed as additional nuisances below.

\subsection{Homogeneous inverse model}"""
replace_once(needle, insert)

needle = r"""\subsection{Deterministic field-gradient contribution}

Expanding Eq.~\eqref{eq:Hpath} gives"""
insert = r"""\subsection{Deterministic field-gradient contribution}

The connection to the local exponent is obtained by expanding the source derivative $P=\partial_zH$.  From Eq.~\eqref{eq:ramoode}, write
\begin{equation}
F(z)=\frac{L-z}{v(z)},
\qquad
I(z)=\int_z^L\frac{L-u}{v(u)}du.
\end{equation}
Then
\begin{equation}
-LP(z,\omega)
=1-i\omega F(z)-\omega^2\frac{I(z)}{v(z)}+O(\omega^3).
\end{equation}
Expanding $\ln[-LP]$ in Eq.~\eqref{eq:gammaloc} therefore gives the point-source coefficients quoted below.  This derivation applies to the local first-difference limit; finite optical kernels are treated separately in Sec.~\ref{sec:kernels}.

Expanding Eq.~\eqref{eq:Hpath} gives"""
replace_once(needle, insert)

replace_once(
    "Thus the false material attribution is controlled by finite support relative to the nuisance region, not by the nominal mean source coordinate.",
    "Thus the controls establish that mean generation depth alone is insufficient and that support inside the nuisance region is a causal contributor to the material-attribution bias.  The exact leakage relation in Eq.~\\eqref{eq:leakage} further shows that the detailed kernel shape within the nuisance region can matter in addition to the total overlap probability.",
)

replace_once(
    "The maximum mean-depth error is $9.3\\times10^{-15}\\,\\mu\\mathrm{m}$, while the recovered diffusion collapses to",
    "The maximum mean-depth error is $9.3\\times10^{-15}\\,\\mu\\mathrm{m}$, while the recovered diffusion collapses to",
)

needle = r"""\begin{equation}
\Deff=-7.7\times10^{-8}\,\mathrm{m^2/s}\simeq0.
\end{equation}
Thus the controls establish"""
insert = r"""\begin{equation}
\Deff=-7.7\times10^{-8}\,\mathrm{m^2/s}\simeq0.
\end{equation}
The exponentially tilted zero-overlap profiles are mathematical causal controls, not a claim that those modified kernel shapes correspond to a realizable optical spectrum or material structure.  Their role is to hold the scalar mean fixed while removing all direct sampling of the nuisance region.
Thus the controls establish"""
replace_once(needle, insert)

needle = r"""\subsection{Covariance-aware multi-frequency rejection}"""
replace_once(
    needle,
    r"\input{PAPER02_SAME_FREQUENCY_STATISTICAL_SNIPPET}" + "\n\n" + needle,
)

replace_once(
    "The preceding results do not depend on HgCdTe specifically. We nevertheless use the existing calibrated HgCdTe kernel construction as a conditional scale example because independently published graded HgCdTe devices provide relevant thickness, field, and timing benchmarks.",
    "The preceding results do not depend on HgCdTe specifically.  This section checks only whether the geometric and effective-field scales used by the conditional stress have independent precedent in HgCdTe; it does not validate the modeled velocity profile or the predicted $D_{\\rm eff}$ for any published detector.  We use the existing theoretical HgCdTe kernel construction as a scale example because independently published graded HgCdTe devices provide relevant thickness, field, and timing benchmarks.",
)

replace_once(
    "The framework has several limitations. The main numerical stress treats deterministic one-carrier transport and omits microscopic diffusion specifically so that the source of $\\Deff$ is unambiguous.",
    "The framework has several limitations. The main numerical stress treats deterministic one-carrier transport and omits microscopic diffusion specifically so that the source of $\\Deff$ is unambiguous.  The optical kernels are likewise treated as known exactly; uncertainty in absorption, composition, interference, wavelength calibration, or the inferred kernel shapes would generate additional channel-space tangent and normal errors and must be propagated in an experiment.",
)

replace_once(
    r"\bibliography{PAPER02_REFERENCES}",
    r"\bibliography{PAPER02_REFERENCES_REV2}",
)

# Add an explicit distinction between descriptive and statistical same-frequency fit quality.
replace_once(
    "The same-frequency calibrated one-mode residual remains of order $10^{-4}$ for the accelerating examples.",
    "The deterministic full-channel relative residual remains of order $10^{-4}$ for the accelerating examples.  Because that normalization includes fitted offset and amplitude components, we do not treat it as a statistical acceptance criterion; the covariance-aware same-frequency test is given in Sec.~\\ref{sec:bias}.",
)

DST.write_text(text, encoding="utf-8")
print(DST)
